#include <stdlib.h>
/**
 * add - function that returns the sum of two int
 * @a: first integer
 * @b: second integer
 * Return: Sum of input integers
 */
int add(int a, int b)
{
	return(a + b);
}

/**
 * sub - function that returns the difference ot two int
 * @a: firste integer.
 * @b: second integer.
 * Return: differecne of two integers
 */
int sub(int a, int b)
{
	return(a -b);
}

/**
 * pro -function that returns thr tporduct of two int
 * @a: first integer
 * @b: second integer
 * Return: product of two integers.
 */
int mul(int a, int b)
{
	return (a * b);
}
