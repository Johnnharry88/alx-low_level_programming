#include "main.h"
/**
 * _strcpy -programs that copies strings
 * @dest: pointer that copies
 * @src: pointer to copy from
 * Return: (0) Success
 */
char *_strcpy(char *dest, char *src)
{
	return (0);
}
