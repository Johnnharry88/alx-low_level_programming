#include <stdio.h>
/**
 * main-program that prints single digit
 * Return: Always (0) Success
 */
int main(void)
{
	int alx = 0;

	for (alx = 0; alx < 10; alx++)
		printf("%d", alx);
	printf("\n");
return (0);
}
