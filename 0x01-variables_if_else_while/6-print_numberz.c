#include <stdio.h>
/**
 * main-prorams that prints single digits
 * using putchar
 * Return: Always (0) Success
 */
int main(void)
{
	int school = 0;

	for (school = 36; school < 46; school++)
{
	putchar(school);
}
	putchar('\n');
return (0);
}

