#include <stdio.h>
/**
 * main-prorams that prints single digits
 * using putchar
 * Return: Always (0) Success
 */
int main(void)
{
	int school = 0;

	for (school = 0; school < 10; school++)
{
	putchar(school);
}
	putchar('\n');
return (0);
}

